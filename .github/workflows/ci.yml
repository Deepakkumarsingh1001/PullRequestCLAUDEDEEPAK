name: CI
on:
  push:
    branches: [main, feature/add-compound-interest]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CI
        shell: pwsh
        run: {ci_command}
